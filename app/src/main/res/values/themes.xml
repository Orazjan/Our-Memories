<resources>

    <style name="Base.Theme.OurMemories" parent="Theme.Material3.DayNight.NoActionBar" />

    <style name="Theme.OurMemories" parent="Base.Theme.OurMemories" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>

        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>

        <item name="postSplashScreenTheme">@style/Theme.OurMemories</item>
    </style>

    <style name="CalendarDay">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="CalenderViewCustom" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/heart_red</item> <!-- Цвет выделения дня -->
        <item name="colorPrimary">@color/heart_red</item> <!-- Цвет текущей даты -->
        <item name="android:textColorPrimary">@color/text_primary</item> <!-- Цвет цифр -->
    </style>


</resources>